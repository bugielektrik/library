# Application Configuration
APP_ENV=development              # Environment: development, staging, production (overrides APP_MODE)
APP_MODE=dev                    # Legacy: use APP_ENV instead
APP_PORT=8080
APP_HOST=0.0.0.0
APP_PATH=/api/v1
APP_TIMEOUT=60s
DEBUG=true                       # Enable debug mode

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=15s
SERVER_WRITE_TIMEOUT=15s
SERVER_SHUTDOWN_TIMEOUT=10s

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=library
DB_USER=library
DB_PASSWORD=library123
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# Legacy PostgreSQL DSN (for backwards compatibility)
POSTGRES_DSN=postgres://library:library123@localhost:5432/library?sslmode=disable&search_path=public

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT/Token Configuration
TOKEN_KEY=your-secret-key-here-change-in-production
TOKEN_EXPIRES=24h
JWT_SECRET=your-jwt-secret-key-here-change-in-production
JWT_EXPIRY=24h

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@library.com

# S3 Storage Configuration
S3_BUCKET=library-files
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key

# Local Storage Configuration
LOCAL_STORAGE_PATH=./uploads
LOCAL_STORAGE_URL=http://localhost:8080/uploads

# Stripe Payment Configuration
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# PayPal Payment Configuration
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
PAYPAL_MODE=sandbox

# Epayment.kz Payment Configuration (Halyk Bank)
EPAYMENT_ENV=test
EPAYMENT_CLIENT_ID=your-epayment-client-id
EPAYMENT_CLIENT_SECRET=your-epayment-client-secret
EPAYMENT_TERMINAL=your-terminal-id
EPAYMENT_BACK_LINK=http://localhost:8080/payment/success
EPAYMENT_POST_LINK=http://localhost:8080/api/v1/payments/callback

# gRPC Configuration
GRPC_PORT=9090

# Logging Configuration
LOG_LEVEL=debug
LOG_FORMAT=json

# Monitoring Configuration
METRICS_ENABLED=true
METRICS_PORT=9091

# Migration Configuration
MIGRATION_PATH=./migrations

# Worker Configuration
WORKER_CONCURRENCY=10
WORKER_QUEUE_NAME=library-tasks

# ===== NEW CONFIGURATION SYSTEM =====
# The application now supports comprehensive configuration management
# Configuration sources (in order of priority):
#   1. Environment variables (highest priority)
#   2. config.{environment}.yaml (e.g., config.production.yaml)
#   3. config.yaml (base configuration)
#   4. Default values (lowest priority)
#
# Configuration Features:
#   - Hot reload support in development mode
#   - Environment-specific overrides
#   - Validation and type safety
#   - Feature flags with dynamic updates
#
# Configuration File Path
CONFIG_PATH=config/config.yaml  # Path to configuration file
#
# To use configuration files:
#   1. Copy config/config.yaml as your base
#   2. Create config/config.production.yaml for production overrides
#   3. Set APP_ENV=production to load production config
#
# Note: Environment variables always override config file values
