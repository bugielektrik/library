# Visual Flows & Diagrams

> **ASCII diagrams showing how data flows through the system**

## 📊 Request Flow Diagrams

### Complete Request Flow: Create a Book

```
┌─────────────────────────────────────────────────────────────────────┐
│ HTTP Layer (Adapters)                                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  POST /api/v1/books                                                │
│  Authorization: Bearer <token>                                     │
│  Body: {"name":"Clean Code","isbn":"9780132350884",...}            │
│                                                                     │
│  ▼                                                                  │
│  ┌───────────────────────────────────────────┐                     │
│  │ Router (router.go)                        │                     │
│  │ - Match route                              │                     │
│  │ - Apply middleware (auth, logging)         │                     │
│  └───────────────────┬───────────────────────┘                     │
│                      ▼                                              │
│  ┌───────────────────────────────────────────┐                     │
│  │ Auth Middleware                           │                     │
│  │ - Verify JWT token                         │                     │
│  │ - Extract claims (user_id, role)           │                     │
│  │ - Add to context                           │                     │
│  └───────────────────┬───────────────────────┘                     │
│                      ▼                                              │
│  ┌───────────────────────────────────────────┐                     │
│  │ BookHandler.CreateBook()                  │                     │
│  │ (handlers/book.go)                        │                     │
│  │                                            │                     │
│  │ 1. Decode JSON → dto.CreateBookRequest     │                     │
│  │ 2. Validate DTO (required fields, format)  │                     │
│  │ 3. Map DTO → Use Case Request              │                     │
│  └───────────────────┬───────────────────────┘                     │
│                      │                                              │
└──────────────────────┼──────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Use Case Layer (Orchestration)                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────┐                     │
│  │ CreateBookUseCase.Execute()               │                     │
│  │ (usecase/bookops/create_book.go)          │                     │
│  │                                            │                     │
│  │ ┌─────────────────────────────────────┐   │                     │
│  │ │ 1. Validate ISBN (Domain Service)   │   │                     │
│  │ │    ▼                                 │   │                     │
│  │ │    BookService.ValidateISBN()        │   │                     │
│  │ │    - Check format (ISBN-10/13)       │   │                     │
│  │ │    - Verify checksum                 │   │                     │
│  │ └─────────────────────────────────────┘   │                     │
│  │                                            │                     │
│  │ ┌─────────────────────────────────────┐   │                     │
│  │ │ 2. Create Entity (Domain)            │   │                     │
│  │ │    ▼                                 │   │                     │
│  │ │    book.NewEntity()                  │   │                     │
│  │ │    - Generate UUID                   │   │                     │
│  │ │    - Set timestamps                  │   │                     │
│  │ └─────────────────────────────────────┘   │                     │
│  │                                            │                     │
│  │ ┌─────────────────────────────────────┐   │                     │
│  │ │ 3. Check Business Rules              │   │                     │
│  │ │    ▼                                 │   │                     │
│  │ │    BookService.CanCreateBook()       │   │                     │
│  │ │    - Check duplicate ISBN            │   │                     │
│  │ └─────────────────────────────────────┘   │                     │
│  │                                            │                     │
│  │ ┌─────────────────────────────────────┐   │                     │
│  │ │ 4. Persist (Repository)              │   │                     │
│  │ │    ▼                                 │   │                     │
│  │ │    BookRepository.Create()           │   │                     │
│  │ └─────────────────────────────────────┘   │                     │
│  │                                            │                     │
│  │ ┌─────────────────────────────────────┐   │                     │
│  │ │ 5. Update Cache                      │   │                     │
│  │ │    ▼                                 │   │                     │
│  │ │    BookCache.Set()                   │   │                     │
│  │ └─────────────────────────────────────┘   │                     │
│  │                                            │                     │
│  │ 6. Return domain.Entity                   │                     │
│  └────────────────┬──────────────────────────┘                     │
│                   │                                                 │
└───────────────────┼─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Adapter Layer (Database)                                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────┐                     │
│  │ PostgresBookRepository.Create()           │                     │
│  │ (repository/postgres/book.go)             │                     │
│  │                                            │                     │
│  │ 1. Map Entity → SQL                        │                     │
│  │ 2. Execute INSERT query                    │                     │
│  │    INSERT INTO books (id, name, isbn, ...) │                     │
│  │    VALUES ($1, $2, $3, ...)                │                     │
│  │ 3. Handle errors → domain errors           │                     │
│  │ 4. Return nil (success)                    │                     │
│  └────────────────┬──────────────────────────┘                     │
│                   │                                                 │
└───────────────────┼─────────────────────────────────────────────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ PostgreSQL Database  │
         │ - books table        │
         │ - Transaction commit │
         └──────────────────────┘
                    │
                    │ Response flows back up
                    ▼
         Handler → Response
         - Map Entity → DTO
         - JSON encode
         - HTTP 201 Created
```

### Authentication Flow

```
┌──────────────────────────────────────────────────────────────┐
│ Login Request                                                │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
         POST /api/v1/auth/login
         {"email":"user@example.com","password":"secret"}
                          │
                          ▼
   ┌────────────────────────────────────────────┐
   │ AuthHandler.Login()                        │
   │ - Validate request                          │
   │ - Call LoginUseCase                         │
   └────────────────┬───────────────────────────┘
                    │
                    ▼
   ┌────────────────────────────────────────────┐
   │ LoginUseCase.Execute()                     │
   │                                             │
   │ 1. Get member by email                      │
   │    ▼                                        │
   │    MemberRepository.GetByEmail()            │
   │    - Query database                         │
   │    - Return member entity                   │
   │                                             │
   │ 2. Verify password                          │
   │    ▼                                        │
   │    PasswordService.Compare()                │
   │    - bcrypt comparison                      │
   │    - Return true/false                      │
   │                                             │
   │ 3. Generate tokens                          │
   │    ▼                                        │
   │    JWTService.GenerateTokenPair()           │
   │    - Access token (24h)                     │
   │    - Refresh token (7d)                     │
   │                                             │
   │ 4. Return tokens + member                   │
   └────────────────┬───────────────────────────┘
                    │
                    ▼
   ┌────────────────────────────────────────────┐
   │ Response                                    │
   │ {                                           │
   │   "member": {...},                          │
   │   "tokens": {                               │
   │     "access_token": "eyJhbGc...",          │
   │     "refresh_token": "eyJhbGc...",         │
   │     "expires_in": 86400                    │
   │   }                                         │
   │ }                                           │
   └────────────────────────────────────────────┘
```

### Protected Endpoint Flow

```
┌──────────────────────────────────────────────────────────────┐
│ Client Request with JWT                                      │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
         GET /api/v1/books
         Authorization: Bearer eyJhbGc...
                          │
                          ▼
   ┌────────────────────────────────────────────┐
   │ Auth Middleware                             │
   │                                             │
   │ 1. Extract token from header                │
   │    - Check "Authorization: Bearer <token>"  │
   │                                             │
   │ 2. Validate token                           │
   │    ▼                                        │
   │    JWTService.ValidateToken()               │
   │    - Verify signature                       │
   │    - Check expiry                           │
   │    - Extract claims                         │
   │                                             │
   │ 3. Add claims to context                    │
   │    - user_id, email, role                   │
   │                                             │
   │ ✓ Token valid → Continue                    │
   │ ✗ Token invalid → 401 Unauthorized          │
   └────────────────┬───────────────────────────┘
                    │
                    ▼ (if valid)
   ┌────────────────────────────────────────────┐
   │ BookHandler.ListBooks()                    │
   │ - User is authenticated                     │
   │ - Proceed with business logic               │
   └────────────────────────────────────────────┘
```

## 🔄 Dependency Injection Flow

### Application Bootstrap

```
┌──────────────────────────────────────────────────────────────┐
│ main() - cmd/api/main.go                                     │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│ app.New() - infrastructure/app/app.go                        │
│                                                               │
│ STEP 1: Infrastructure Layer                                 │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ 1. Logger                                               │  │
│ │    log.Init()                                           │  │
│ │                                                          │  │
│ │ 2. Config                                               │  │
│ │    config.Load() → Database DSN, JWT Secret, etc.       │  │
│ │                                                          │  │
│ │ 3. Database                                             │  │
│ │    store.NewPostgres(dsn)                               │  │
│ │    - Connect to PostgreSQL                              │  │
│ │    - Run migrations automatically                       │  │
│ │                                                          │  │
│ │ 4. Cache                                                │  │
│ │    store.NewRedis(host) OR memory cache                 │  │
│ │                                                          │  │
│ │ 5. Auth Services                                        │  │
│ │    auth.NewJWTService(secret)                           │  │
│ │    auth.NewPasswordService()                            │  │
│ └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│                          ▼                                    │
│ STEP 2: Adapter Layer (Repositories)                         │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ repos := usecase.Repositories{                          │  │
│ │     Book:   postgres.NewBookRepository(db),             │  │
│ │     Member: postgres.NewMemberRepository(db),           │  │
│ │     Author: postgres.NewAuthorRepository(db),           │  │
│ │ }                                                        │  │
│ └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│                          ▼                                    │
│ STEP 3: Adapter Layer (Caches)                               │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ caches := usecase.Caches{                               │  │
│ │     Book:   redis.NewBookCache(cache),                  │  │
│ │     Author: redis.NewAuthorCache(cache),                │  │
│ │ }                                                        │  │
│ └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│                          ▼                                    │
│ STEP 4: Use Case Layer (Container)                           │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ container := usecase.NewContainer(repos, caches, authSvc)│  │
│ │                                                          │  │
│ │ Inside NewContainer():                                  │  │
│ │   - Create domain services (book.NewService())          │  │
│ │   - Wire use cases with dependencies                    │  │
│ │   - Return Container with all use cases                 │  │
│ └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│                          ▼                                    │
│ STEP 5: HTTP Layer (Handlers & Router)                       │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ handlers := http.NewHandlers(container)                 │  │
│ │ router := http.NewRouter(handlers)                      │  │
│ │                                                          │  │
│ │ Routes configured with middleware                       │  │
│ └─────────────────────────────────────────────────────────┘  │
│                          │                                    │
│                          ▼                                    │
│ STEP 6: Start Server                                         │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ server.Start(":8080", router)                           │  │
│ └─────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### Container Wiring Detail

```
usecase.NewContainer(repos, caches, authSvcs)
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│ internal/usecase/container.go                                │
│                                                               │
│ func NewContainer(...) *Container {                          │
│                                                               │
│   // Create domain services (NO external dependencies)       │
│   bookService := book.NewService()                           │
│   memberService := member.NewService()                       │
│                                                               │
│   return &Container{                                         │
│     // Book use cases                                        │
│     CreateBook: bookops.NewCreateBookUseCase(                │
│         repos.Book,      // Repository interface             │
│         caches.Book,     // Cache interface                  │
│         bookService,     // Domain service                   │
│     ),                                                        │
│                                                               │
│     GetBook: bookops.NewGetBookUseCase(                      │
│         repos.Book,                                          │
│         caches.Book,                                         │
│     ),                                                        │
│                                                               │
│     // Auth use cases                                        │
│     RegisterMember: authops.NewRegisterUseCase(              │
│         repos.Member,          // Repository                 │
│         authSvcs.Password,     // Infrastructure service     │
│         authSvcs.JWT,          // Infrastructure service     │
│         memberService,         // Domain service             │
│     ),                                                        │
│                                                               │
│     // ... other use cases                                   │
│   }                                                           │
│ }                                                             │
└──────────────────────────────────────────────────────────────┘
```

## 📐 Layer Dependency Graph

```
┌─────────────────────────────────────────────────────────────┐
│                      DEPENDENCY FLOW                         │
│                                                              │
│  External          Infrastructure      Adapters             │
│  Frameworks        (Technical)         (Interfaces)         │
│      │                  │                  │                │
│      │                  │                  │                │
│      ▼                  ▼                  ▼                │
│  ┌────────┐      ┌────────────┐    ┌─────────────┐         │
│  │  HTTP  │──────│ Auth/Log   │────│  Handlers   │         │
│  │  Chi   │      │ JWT/Zap    │    │  Router     │         │
│  └────────┘      └────────────┘    │  DTOs       │         │
│                                     └──────┬──────┘         │
│                                            │                │
│                                            │ Uses           │
│                                            ▼                │
│                  ┌──────────────────────────────────┐       │
│                  │     Use Case Layer               │       │
│                  │  (Business Orchestration)        │       │
│                  │                                  │       │
│                  │  bookops, authops, subops        │       │
│                  └────────────┬─────────────────────┘       │
│                               │                             │
│                               │ Depends on                  │
│                               ▼                             │
│           ┌──────────────────────────────────────┐          │
│           │      Domain Layer                    │          │
│           │  (Business Logic - PURE)             │          │
│           │                                      │          │
│           │  Entities, Services, Interfaces      │          │
│           │  NO external dependencies            │          │
│           └──────────────────────────────────────┘          │
│                                                              │
│  Key Rules:                                                  │
│  • Domain imports NOTHING from outer layers                  │
│  • Use cases import only domain interfaces                   │
│  • Adapters implement domain interfaces                      │
│  • Infrastructure can depend on any layer                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🔍 Package Import Rules

```
✅ ALLOWED                          ❌ FORBIDDEN

Domain Layer:
✓ Standard library                  ✗ Use case layer
✓ Third-party utilities             ✗ Adapter layer
  (uuid, time, etc.)                ✗ Infrastructure layer
                                    ✗ HTTP frameworks
                                    ✗ Database drivers

Use Case Layer:
✓ Domain interfaces                 ✗ Specific repository implementations
✓ Domain entities                   ✗ HTTP handlers
✓ Domain services                   ✗ Database code
                                    ✗ Framework code

Adapter Layer:
✓ Domain interfaces                 ✗ Other adapters
✓ Domain entities                   ✗ Infrastructure (usually)
✓ Framework code
✓ Database drivers

Infrastructure Layer:
✓ Everything                        (No restrictions - cross-cutting)
```

## 🎯 Use Case Execution Pattern

```
Every use case follows this pattern:

┌────────────────────────────────────────┐
│ UseCase.Execute(ctx, request)          │
│                                         │
│ 1. ┌──────────────────────────────┐    │
│    │ Validate Input                │    │
│    │ - Use domain service          │    │
│    │ - Check business rules        │    │
│    └──────────────┬────────────────┘    │
│                   │                     │
│                   ▼                     │
│ 2. ┌──────────────────────────────┐    │
│    │ Create/Get Entity             │    │
│    │ - Use repository              │    │
│    │ - Check cache                 │    │
│    └──────────────┬────────────────┘    │
│                   │                     │
│                   ▼                     │
│ 3. ┌──────────────────────────────┐    │
│    │ Apply Business Logic          │    │
│    │ - Call domain service methods │    │
│    │ - Transform data              │    │
│    └──────────────┬────────────────┘    │
│                   │                     │
│                   ▼                     │
│ 4. ┌──────────────────────────────┐    │
│    │ Persist Changes               │    │
│    │ - Call repository             │    │
│    │ - Handle errors               │    │
│    └──────────────┬────────────────┘    │
│                   │                     │
│                   ▼                     │
│ 5. ┌──────────────────────────────┐    │
│    │ Update Cache/Notify           │    │
│    │ - Invalidate/update cache     │    │
│    │ - Trigger events (if any)     │    │
│    └──────────────┬────────────────┘    │
│                   │                     │
│                   ▼                     │
│ 6. Return domain.Entity or error       │
│                                         │
└────────────────────────────────────────┘
```

## 🗄️ Database Query Flow

```
┌──────────────────────────────────────────────────────────────┐
│ Repository Method Call                                       │
│ repo.GetByID(ctx, "book-123")                               │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
   ┌────────────────────────────────────────────┐
   │ PostgresBookRepository.GetByID()           │
   │                                             │
   │ 1. Prepare SQL query                        │
   │    query := `                               │
   │      SELECT id, name, isbn, genre,          │
   │             created_at, updated_at          │
   │      FROM books                             │
   │      WHERE id = $1                          │
   │    `                                        │
   │                                             │
   │ 2. Execute query                            │
   │    row := r.db.QueryRowContext(ctx, query, id)│
   │                                             │
   │ 3. Scan result                              │
   │    var dbBook dbModel                       │
   │    err := row.Scan(&dbBook.ID, ...)        │
   │                                             │
   │ 4. Map DB model → Domain entity             │
   │    entity := book.Entity{                   │
   │        ID:   dbBook.ID,                     │
   │        Name: dbBook.Name,                   │
   │        ...                                  │
   │    }                                        │
   │                                             │
   │ 5. Handle errors                            │
   │    - sql.ErrNoRows → errors.ErrNotFound    │
   │    - Other errors → wrapped error           │
   │                                             │
   │ 6. Return entity                            │
   └────────────────┬───────────────────────────┘
                    │
                    ▼
         Return domain.Entity
```

## 🧪 Test Flow

```
Unit Test (Domain Service):
─────────────────────────────
No external dependencies!

┌────────────────────────────┐
│ TestValidateISBN           │
│                             │
│ svc := NewService()         │ ← Direct instantiation
│ err := svc.ValidateISBN()   │ ← Pure function call
│ assert.NoError(err)         │ ← Immediate verification
└────────────────────────────┘

Integration Test (Use Case):
────────────────────────────
Uses mocks for dependencies

┌────────────────────────────────────────┐
│ TestCreateBookUseCase                  │
│                                         │
│ 1. Setup mocks                          │
│    mockRepo := &MockRepository{}        │
│    mockRepo.CreateFunc = func(...) {}  │
│                                         │
│ 2. Create use case with mocks           │
│    uc := NewCreateBookUseCase(          │
│        mockRepo,                        │
│        mockCache,                       │
│        service                          │
│    )                                    │
│                                         │
│ 3. Execute                              │
│    result, err := uc.Execute(req)       │
│                                         │
│ 4. Verify                               │
│    assert.NoError(err)                  │
│    assert.Equal(expected, result)       │
│    assert.True(mockRepo.CreateCalled)   │
└────────────────────────────────────────┘

Integration Test (Repository):
───────────────────────────────
Uses real database

//go:build integration

┌────────────────────────────────────────┐
│ TestBookRepository_Create              │
│                                         │
│ 1. Setup test database                  │
│    db := testdb.Setup(t)                │
│                                         │
│ 2. Create repository                    │
│    repo := postgres.NewBookRepository(db)│
│                                         │
│ 3. Execute operation                    │
│    err := repo.Create(ctx, book)        │
│                                         │
│ 4. Verify in database                   │
│    row := db.QueryRow("SELECT ...")     │
│    assert.Equal(book.ID, result.ID)     │
│                                         │
│ 5. Cleanup                              │
│    defer testdb.Teardown(t, db)         │
└────────────────────────────────────────┘
```

## 📖 Quick Reference

### Layer Responsibilities

| Layer | Responsibility | Example |
|-------|----------------|---------|
| **Domain** | Business rules, pure logic | ISBN validation, late fee calculation |
| **Use Case** | Orchestration, workflow | Validate → Create → Save → Cache |
| **Adapter** | External interfaces | HTTP handlers, DB queries, DTOs |
| **Infrastructure** | Technical concerns | JWT, logging, database connection |

### Data Transformation Points

```
HTTP Request (JSON)
        ↓
    DTO (adapter)
        ↓
Use Case Request
        ↓
Domain Entity ←──── Repository returns Entity
        ↓
    DTO (adapter)
        ↓
HTTP Response (JSON)
```

### Error Flow

```
Repository Error (sql.ErrNoRows)
        ↓
Domain Error (errors.ErrNotFound)
        ↓
Use Case (wrapped: "getting book: %w")
        ↓
Handler (HTTP status code: 404)
        ↓
Client (JSON error response)
```

---

**Use these diagrams to understand how components connect!** 🔍
